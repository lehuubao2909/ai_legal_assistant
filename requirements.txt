# ============================================================================
# R2AI 2026 — AI Legal Assistant: dependencies TÁI HIỆN pipeline thi
# Môi trường chuẩn: Kaggle Notebook, Accelerator = GPU T4 x2, Internet ON,
# Python 3.10/3.11. (Local dev Mac/Linux Python 3.10+ chạy được nhưng chậm.)
# Cài: pip install -r requirements.txt
# ============================================================================

# ---- Retrieval core (BGE-M3 embedding + cross-encoder reranker) ----
sentence-transformers>=3.0.0
transformers>=4.44.0
torch>=2.2.0
accelerate>=0.30.0

# ---- Sparse retrieval + fusion ----
rank-bm25>=0.2.2
numpy>=1.24

# ---- Corpus build từ HuggingFace dataset (tmquan/vbpl-vn) ----
datasets>=2.18.0

# ---- Tách từ tiếng Việt cho BM25 (TÙY CHỌN — pipeline chạy được nếu thiếu) ----
pyvi>=0.1.1

# ============================================================================
# CẢI TIẾN (TÙY CHỌN) — fine-tune reranker. Chỉ cần khi chạy
# kaggle/finetune_reranker_kaggle.ipynb. KHÔNG bắt buộc để tái hiện 0.5766.
#   pip install "FlagEmbedding>=1.3" deepspeed
# ============================================================================

# ============================================================================
# LLM sinh đáp án (QA — KHÔNG ảnh hưởng ARTICLES_F2):
#   - Kaggle: Qwen/Qwen2.5-7B-Instruct tải qua transformers (đã có ở trên).
#   - Local : Qwen2.5-7B qua Ollama (ngoài pip): ollama pull qwen2.5:7b-instruct-q4_K_M
# ============================================================================

# GHI CHÚ: backend/requirements.txt (FastAPI, google-antigravity) là cho BẢN WEB-APP
# giai đoạn sau — KHÔNG dùng cho pipeline thi, đừng cài cho mục đích tái hiện.
